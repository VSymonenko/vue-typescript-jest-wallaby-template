/**
 * Mount element for bypass already tested elements
 * Elements maintained by a contributor of libraries
 */
import Vue from 'vue';
import Vuetify from 'vuetify';
import '@/router';

Vue.use(Vuetify, {
  iconfont: 'mdi',
});
